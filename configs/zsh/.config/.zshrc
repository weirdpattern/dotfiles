for aliases in ~/.config/zsh/aliases/*.sh(N.); do 
  source "$aliases"; 
done

for exports in ~/.config/zsh/aliases/*.sh(N.); do 
  source "$exports"; 
done

for registrations in ~/.config/zsh/aliases/*.sh(N.); do 
  source "$registrations"; 
done
